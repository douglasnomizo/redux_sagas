// import { fromJS } from 'immutable';
// import { makeSelectAdoptionPageDomain } from '../selectors';

// const selector = makeSelectAdoptionPageDomain();

describe('makeSelectAdoptionPageDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
